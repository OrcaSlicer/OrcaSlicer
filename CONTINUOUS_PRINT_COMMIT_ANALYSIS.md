# 零空驶连续打印分支：提交、实现与算法分析

| 项           | 值                                                                                                                |
| ------------ | ----------------------------------------------------------------------------------------------------------------- |
| 分支         | `feature/continuous-print-zero-travel`                                                                          |
| HEAD         | `cd328070cb`（2026-09-15）                                                                                      |
| 功能提交区间 | `8500fcdcca..cd328070cb`（9 个提交，2026-09-08 ~ 2026-09-15）                                                   |
| 代码基线     | OrcaSlicer v2.4.2 快照（上游`8500fcdcca`）                                                                      |
| 改动规模     | 23 个文件，+3724 / −76 行                                                                                        |
| 配套文档     | `CONTINUOUS_PRINT_ZERO_TRAVEL_DESIGN.md`（设计）、` CONTINUOUS_PRINT_IMPLEMENTATION_CHECKLIST.md`（实施清单） |

> 说明：`git log main..HEAD` 会列出 85 个提交，其中 76 个是上游 `release/v2.4` 的历史（作者为 SoftFever 等），不属于本分支的工作。本报告只分析作者为「小颜 / Gogs」的 9 个功能提交。

---

## 目录

- [1. 提交一览](#1-提交一览)
- [2. 逐提交分析](#2-逐提交分析)
- [3. 算法详解](#3-算法详解)
- [4. 已实现功能](#4-已实现功能)
- [5. 重难点](#5-重难点)
- [6. 已知限制与未完成项](#6-已知限制与未完成项)
- [7. 附录：文件与命令](#7-附录文件与命令)

---

## 1. 提交一览

| # | Commit         | 日期  | 主题                                      | 规模         |
| - | -------------- | ----- | ----------------------------------------- | ------------ |
| 1 | `7a6440ca64` | 09-08 | docs: 设计文档、实施清单与参数说明        | +835         |
| 2 | `3a85d52a56` | 09-08 | feat: M1 精确链化算子 + 层内判定器 + 单测 | +503         |
| 3 | `f213ab793f` | 09-08 | docs: 新增 3.5 墙-填充接合机制            | +55 / −33   |
| 4 | `ea471a382b` | 09-08 | feat: M2 接合点拆分算子 + 文本过滤器原型  | +694 / −62  |
| 5 | `4de1877c98` | 09-08 | docs: 清单勾选 M2；记录实测数据           | +18 / −11   |
| 6 | `7c99a71f73` | 09-12 | feat: M3 管线接入 + M4 零空驶验证工具     | +1026 / −56 |
| 7 | `9a78619682` | 09-12 | docs: 清单勾选 M3/M4                      | +50 / −24   |
| 8 | `157c55a497` | 09-15 | fix: 多墙连续打印与实心层沿墙衔接         | +899 / −58  |
| 9 | `cd328070cb` | 09-15 | fix: 保留真实层界终点并改为固定层高       | +301 / −489 |

整体演进路径与设计文档第 6 章的 M1→M4 里程碑一致：先做**独立可测的数据层算子与判定器**（不碰管线），再做**离线过滤器原型**，然后**接入切片管线**，最后用**离线 G-code 审计工具**做端到端验证；此后两轮 fix 由实测暴露的几何问题驱动，并把层间方案从"螺旋连续 Z"改为"固定层高 + XY 连续"。

---

## 2. 逐提交分析

### 2.1 `7a6440ca64` — 设计文档与实施清单（纯文档）

新增三份文档，确立整个工作的前提与边界：

- `CONTINUOUS_PRINT_ZERO_TRAVEL_DESIGN.md`（547 行）：把问题形式化为**无向图上的欧拉迹**（顶点 = 挤出段端点，边 = 挤出段），给出"层内零空驶一笔覆盖 ⇔ 奇度顶点数为 0 或 2"的判据；梳理现有代码地图（GCode 主管线、`ExtrusionEntity` 体系、SpiralVase 四步机制、SeamPlacer）；列出 6 项缺口 G1–G6 与接入方案。
- `CONTINUOUS_PRINT_IMPLEMENTATION_CHECKLIST.md`：M1–M4 里程碑与出口标准、构建注意事项。
- `Parameter Description.md`：`build.bat` 参数解析（`pack`/`debug`/`debuginfo`/`x64`/`arm64`/`deps`/`slicer` 与构建目录对照表），作为环境准备参考。

关键设计决策（后续实现遵循）：新功能默认关闭；SpiralVase 的"单 perimeter 单环"被泛化为**任意单链连续迹（开放或闭合）**；层间只要求"上一层终点 = 本层起点"（转移点不必是外壁 seam，方向可逐层反向）。

### 2.2 `3a85d52a56` — M1：精确链化算子 + 判定器 + 单测

纯新增，不触碰 `GCode.cpp` / `PrintConfig.cpp`，既有行为零变化。

- `ShortestPath.hpp/.cpp`：新增 `ExactChainResult` 与 `chain_extrusion_entities_exact()`（`ShortestPath.cpp:1078`）。
- `GCode/ContinuousPrint.hpp/.cpp`（新增）：`ContinuousPrintVerdict`、`ContinuousLayerPlan`（顺序/起终点/闭合标志/总长 mm/1mm 弧长采样）、`preflight_layer()` 骨架。
- `tests/libslic3r/test_continuous_print.cpp`（新增，并在 CMake 文件清单登记）。

出口标准：单测全过（空集/单环/平行线/端接折线链/需反转的中间段/分离环/`preferred_start` 选择等）、`SCALED_EPSILON` 下"绝不创造连线"。

### 2.3 `f213ab793f` — 文档：3.4 连续填充图案与 3.5 lollipop 接合

把可行域从"薄壁壳"扩展到**实心顶/底与稀疏填充层**：`top/bottom_surface_pattern=monotonic`、`sparse_infill_pattern=alignedrectilinear`、`internal_solid_infill_pattern=monotonic` 这些图案在生成阶段就把平行线沿内轮廓连成单条迹。由此引出 3.5 节的 **lollipop 接合机制**：填充迹端点落在墙 loop 上时，把 loop 在该点**拆分**（度数 3 + 远端度数 1 = 恰好 2 个奇度顶点），从而存在开放欧拉迹；强调**拆分 ≠ 补线**（不新增几何，只是重新划分顶点）。

### 2.4 `ea471a382b` — M2：接合点拆分算子 + 文本过滤器原型

- `split_entities_at_junctions()`（`ContinuousPrint.cpp:275`）：检测"实体 A 的端点落在实体 B 的中间（ε 内）"，把 B 在接合点处拆开，并把接触端点**吸附**到接合点上（位移 ≤ 容差，不新增线段）；`ExtrusionPath` 直接切分，`ExtrusionLoop` 在接合点线性化；拆分件继承源路径的 role/宽度/层高/流量属性。
- `ContinuousPrint` 过滤器类：泛化 `SpiralVase::process_layer` 的四步机制（首段 Z 改写、Z-ramp、XY 平滑滑移、travel/回抽过滤）到任意单链连续迹；`SpiralVaseHelpers` 迁移到 `SpiralVase.hpp`（inline）供两者共用。
- 单测：lollipop 通过、双接合点桥接通过、T 型接合（4 奇度）拒绝、闭合环输入下与 SpiralVase **逐字节等价**、零空驶断言、Z 单调递增。

> 注：这个 `ContinuousPrint` 过滤器类在第 9 个提交中被整体删除（见 2.9），但 `SpiralVaseHelpers` 的迁移保留了下来。

### 2.5 `4de1877c98` — 文档：M2 出口记录

记录 Release 构建可用与 CLI 切片用法（`--slice` 需带板号），并给出连续填充图案的实测空驶统计（20mm 实心立方体）：`alignedrectilinear` 0/1071、`monotonic` 顶 0/141 底 0/122、内部实心 2/474；对照 Inner wall 50/200（两条墙环之间不共点，符合 lollipop 预期）。

### 2.6 `7c99a71f73` — M3：管线接入 + M4 审计工具（本轮最大提交）

**配置层**

- `PrintConfig` 新增 `continuous_print_mode`（bool，默认 false，comAdvanced）与 `continuous_print_max_join_distance`（float，默认 **2.0mm**，0 = 严格不补线）。
- `Preset::print_options()` 登记这两个键——清单里明确记录：**只加 def + GUI 行而不登记会导致点「Others」设置页崩溃**（页面对缺失键取值为 null）。
- `normalize_fdm` / `normalize_fdm_1`：开启时强制关闭 `retract_when_changing_layer` 与 `filament_retract_when_changing_layer`，理由是连续打印靠挤出段焊接层界、从不 travel，被过滤器丢弃的回抽会留下未配对的回填。

**管线层（`GCode.cpp` / `GCode.hpp`）**

- `_do_export`（`GCode.cpp:2564`）：结构门 `continuous_print_compatible()`（`GCode.cpp:5648`，单对象单实例、单材料/单喷嘴、`ByLayer`、无擦料塔）通过才置 `m_continuous_print = true`；与 `spiral_mode` 互斥（spiral 优先）。
- `process_layer` 发射段（`GCode.cpp:5475`）：按层调用 `preflight_layer`，命中时整层走 `emit_continuous_print_layer()`（`GCode.cpp:5665`）发射为单条连续链，失败层**逐层回退**常规发射；`preferred_start` 传上一层终点（首层用当前喷嘴位置）。
- 发射时临时关闭 `enable_arc_fitting`，保证后续按 G1 顶点计算与审计的准确性，退出时用 `ScopeGuard` 还原。
- `change_layer` / `retract(lazy_lift)`：连续模式下与 spiral 同法走普通 Z 提升（否则过滤器会把 z-hop 的 Z 误当层高基准）。
- 用户提示：`GCodeProcessorResult::continuous_print_report` 携带整单结论，经 GUI 通知（`Plater.cpp`）、CLI stderr、日志三处暴露；三种文案——结构门不通过 / 无任何层可一画 / 部分层不可一画（N/M）。

**算法层（本轮的关键修复）**

- **接合容差改为物理量**：真实切片中墙与填充之间存在 `infill_wall_overlap` 级别的缝（顶/底面 ≈0.12mm，内部实心 ≈0.48mm），原用 `SCALED_EPSILON`（≈1e-4mm）判定接合导致**特征从不生效**。改为 `0.5 × 喷嘴直径` 容差 + 端点吸附。
- **多墙一笔画**：排序器改为"按角色分组 + 墙环按面积降序（墙1→墙2→…）+ 方向由上一层终点自动选择"，实测逐层交替 `Outer wall → Inner wall → Sparse infill` 与反向；闭环置缝改为**在边上插入顶点的最近点置缝**（此前只吸附到已有顶点，0.12mm 的缝无法精确落点）。
- **有界连接段**：间隙 > 容差且 ≤ `max_join` 时插入直线挤出连接（继承相邻实体属性），用于墙↔墙、墙↔填充/支撑；超上限则整层回退。
- **墙→顶/底面用"不完整墙壁"衔接**：不再插入独立连接实体，而是把填充起点**追加到墙折线末尾**（沿墙切向延伸）。
- `CP_DEBUG=1` 诊断开关：打印每层实体清单（role/闭环/点数）与失败 hop。

**验证层**

- `tools/continuous_print_check.py`（M4）：解析 G-code，统计层内空驶、层界空驶、断链、回抽、Z 回退、层内 Z 是否平整。

实测：1 墙+monotonic 顶底+alignedrectilinear 47/49 层零空驶；2 墙 45/49；纯薄壁 50/50；内部实心填充层因填充实体相距可达 24mm 而回退。

### 2.7 `9a78619682` — 文档：M3/M4 出口记录

记录上述关键修复（容差、多墙、顶底面、`join` 默认 1.0→2.0mm、`Preset::print_options` 崩溃坑）与仍回退的层类型。

### 2.8 `157c55a497` — 多墙连续打印与实心层沿墙衔接

这一轮修正了"零空驶 ≠ 几何正确"的问题：此前第 3 层仍存在从外墙直连填充入口的长斜线（跨越实心区域的悬空挤出）。

- **有界连接模式保留完整墙环**：不再在多个填充端点处拆分墙环（拆分会把墙变成互不相关的开放片段，按面积排序出错）；严格模式（`max_join=0`）仍使用接合点拆分。
- **实心层沿轮廓接入**（`build_contour_connected_chain()`，`ContinuousPrint.cpp:868`）：喷嘴在外墙时依次打印外墙、内墙，再沿最内墙内侧的等距轮廓生成**额外墙弧**接入填充；喷嘴已在内部时可先入填充再由内向外打印墙。
- **局部填充避让**（`inset_surface_paths()`，`ContinuousPrint.cpp:813`）：按延伸墙与填充线宽之和的一半生成避让带，裁短进入该带的扫描线，沿剩余区域边界连接折返与短填充连接段；完全被替代的缝隙残段移除。
- **逐段几何校验**：验证填充位于避让后区域内（坐标容差 0.002mm）且整条输出链首尾精确相接，不再只看空驶计数。
- 入口候选：最近的填充段可能位于可连通链的中部，当前入口失败时尝试其他端点/种子再经墙弧接入，避免多墙实心层因贪心排序误回退。

实测：圆柱体 130/130 层零层内与层间空驶；第 4–125 层（122 层）的逐段 XY/Z/E/角色与本轮修改前**完全一致**；关闭功能输出一致。专项测试 17 项 803 断言通过。

### 2.9 `cd328070cb` — 保留真实层界终点并改为固定层高

这一轮按用户最新要求推翻了原设计中的"连续 Z"方案：

- **删除实心层为保留旧衔接点而追加的收尾**：层计划终点必须是真实最后一笔的终点，不再为保持旧输出而多画约 0.947mm 到外墙。
- **移除连续打印专用的螺旋文本过滤器**（`ContinuousPrint` 类及其状态、`LayerResult::continuous_print_enable`、管道分派），每层按正常层高发射，换层纯 Z 抬升；同时移除 `spiral_mode_smooth` 相关的转移点预算校验与 GUI 联动。
- **短层界连接与沿轮廓接入纳入下一层几何计划**：第 4 层从第 3 层真实终点升层后沿内侧轮廓接入并局部避让；回退时清除过期终点（改用实际喷嘴位置），避免用错误的 `preferred_start`。
- 首层不再排除裙边/底边（删掉 `layer_has_setup_geometry` 条件）；发射期间关闭圆弧拟合以保持连接顶点显式。
- `tools/continuous_print_check.py` 改为检查**全部模型层与固定 Z**（不再按 Z-ramp 挑连续区），支持模态角色、`G92`、绝对/相对坐标与 E、`G2/G3`、跨层两侧空驶；新增 `tests/continuous_print_check_tests.py`（5 用例）。
- 新增 python 审计测试，重写 C++ 测试（现 8 个 `TEST_CASE`、133 处断言宏）。

实测：六棱柱 75/75 层、原圆柱体 130/130 层均为固定 Z、零层内与层间 XY 空驶、零 Z 回退；六棱柱第 5–69 层 XY/E/角色与用户原始文件逐段一致；关闭功能时输出与基线一致。

---

## 3. 算法详解

### 3.1 问题建模与可行性边界

把一层的挤出轨迹视为无向图：顶点 = 端点坐标，边 = 挤出段。则

- **层内零空驶一笔覆盖 ⇔ 图存在欧拉迹**，即奇度顶点数为 **0（闭合迹）或 2（开放迹）**；> 2 则不可一笔画。
- 3D 打印的额外现实：不同挤出段之间**极少恰好端点重合**（平行填充线彼此分离），且经典补救手段（中国邮路问题：用重复边连接奇点）在打印语境里等价于"重复挤出或补连接线"。

因此可行域被收敛为两类：**薄壁单链**（天然端点相接）与**原生连续图案**（生成阶段就把平行线连成单条迹的 monotonic / alignedrectilinear）；普通 rectilinear 填充在"不补线"前提下从根上不可行。

### 3.2 精确链化：并查集合并顶点 + Hierholzer 求欧拉迹

`chain_extrusion_entities_exact()`（`ShortestPath.cpp:1078`）：

1. 每个实体提供两个端点槽位（`2i` = 首点，`2i+1` = 末点）；用**并查集**把 `is_approx(..., SCALED_EPSILON)` 的端点合并为同一顶点；闭合实体（首末重合）两槽位自合并。
2. 统计各顶点度数；奇度顶点 0 个 → 闭合链，2 个 → 开放链（起终点即这两个奇点），否则返回 `std::nullopt`。
3. 起点选择：开放链取两个奇点中离 `preferred_start` 较近者；闭合链取离 `preferred_start` 最近的顶点。
4. **Hierholzer 算法**（迭代式）求欧拉迹：沿未使用的边深度优先走，无路可走时把顶点压入回路并回溯，最后反转得到边序列。
5. **连通性不做独立判定**，由"消费边数 == 实体数"兜底：图不连通时必然消费不完，直接返回 `std::nullopt`。
6. 按回路方向标记每段是否需要反转；需要反转但 `can_reverse()` 为假时保守返回失败。

该算子**绝不创造连线**，是"不补线"约束的形式化保证。

### 3.3 接合点拆分与端点吸附（lollipop）

`split_entities_at_junctions()`（`ContinuousPrint.cpp:275`）解决"墙 loop + 连续填充迹初始不连通"的问题：

1. 用 `flatten_entity()` 把 `ExtrusionPath` / `ExtrusionLoop` 展平成统一的多段线表示，并记录每个**线段**的来源路径索引（用于拆分后继承属性）；loop 显式闭合且 `closed=true`。
2. **弧长参数化**：`arc_prefix()` 计算前缀弧长；`closest_s_on_polyline()` 求点在折线上的最近位置（**投影到线段，不只是顶点**）与距离。
3. 对每个实体 i，遍历其他实体的两个端点，若距离 ≤ `junction_epsilon` 且落点在内部（非端点附近），记录为接合位置，并生成一条"端点吸附"记录：把该端点**移动到**接合点（位移 ≤ 容差，不新增线段）。
4. 应用吸附（Path / MultiPath 的首末点），再按接合位置排序去重后拆分：
   - 开放折线：`split_open()` 按弧长切分；
   - 闭合环：先用 `rotate_ring()` 在第一个接合点处**旋转线性化**（把环变成起终点相同的开放折线，接合点处精确插值落点），其余接合点做坐标平移后切分。
5. `emit_piece()` 把切分结果按来源路径分组发回 `ExtrusionPath`，保证 role / 宽度 / 层高 / 流量等属性逐段继承。

数学效果：接合点度数 = 3（墙进、墙出、填充），填充远端度数 = 1，恰好 2 个奇度顶点 → 存在开放欧拉迹（棒棒糖图）。

### 3.4 有界连接贪心单链排序

`build_chain_with_connectors()`（`ContinuousPrint.cpp:515`）是用户授权"有界挤出连接"后的主力排序器：

- **角色分组**：按 role 分成墙组与填充组；墙按轮廓面积降序 = 外墙→内墙（保证由外向内）。
- **强制置缝传播**：取填充迹的可用端点作为入口，从最内墙向外墙逐圈计算"目标点在墙上的最近投影"，把每圈墙的 seam 强制对齐到该目标；`reseam_closed()` 在**边上插入顶点**精确置缝（此前只能吸附到已有顶点，0.12mm 的墙-填充缝永远落不上），使最内墙的终点正好是填充的起点。
- **方向交替**：以 `preferred_start`（上一层终点）分别到墙首点与填充端点的距离决定"墙先"还是"填充先"，从而逐层交替 `墙→填充` / `填充→墙`，同时让层间焊缝最短。
- **填充排序**：`order_fills()` 按到当前游标的最近距离贪心排序（闭合填充按投影距离），内部实心被切成 2–3 段时可用短连接一次走完。
- **连接生成**：相邻两段的 hop > 容差且 ≤ `max_join` 时插入直线连接段（`make_connector_path()`，继承相邻实体属性）；超过上限返回失败。
- **墙→非墙的"不完整墙壁"**：衔接处不插独立连接实体，而是把下一段起点**追加到上一段墙折线的末尾**，视觉上读作墙沿切向延伸进顶/底面。
- 失败后的**二次尝试**：先用原始顺序试一次（避免过早 splice 把可达的填充端点弄丢），失败才调用 `splice_surface_fragments()` 再做一次。

### 3.5 实心层沿轮廓接入与局部避让

`build_contour_connected_chain()`（`ContinuousPrint.cpp:868`）是本分支最复杂的一块：

1. 输入按 role 分为墙与填充；要求墙为闭合、填充非空，否则拒绝。
2. 取**最内墙**轮廓，按 `Flow::rounded_rectangle_extrusion_spacing(宽度, 层高)` 做负偏移得到 `guide` 轮廓（额外墙弧的路径）。偏移结果必须是单连通多边形，否则拒绝。
3. 由 `preferred` 到墙/填充的距离决定 `walls_first`。
4. **入口搜索**（`order_fill()`）：先直接链化；失败则插入局部缝隙残段再链；仍失败则按距离排序尝试其他端点/中点作为种子——解决"最近段位于可连通链的中部"导致的误回退。
5. **避让带**：用 `contour_section()` 求 `guide` 上从当前游标到填充入口的**较短弧段** `extension`，按 `0.5 × (墙宽 + 填充宽)` 生成开轮廓偏移带，与轮廓 `diff_ex` 得到剩余区域；丢弃不含任何填充的空小袋（尖角偏移残留），但**只要剩余区域不止一个或带孔就拒绝**（多连通说明连接会跨区）。
6. **局部裁剪**（`inset_surface_paths()`，`:813`）：逐段与剩余区域求交，把落在带内的扫描线裁短；边界处的折返用 `contour_section()` 沿剩余区域边界接回去，保持每段仍然是单条连通迹；完全落在带外的实体删除。
7. **校验**：把局部连接段的首末点对齐到同一避让边界（含 `SCALED_EPSILON` 级的接触精确化），再逐条检查 (a) 相邻段首尾**精确相接**，(b) 整条填充线位于剩余区域（容差 0.002mm）内。任一不满足即整层回退——这就是清单里"零空驶不代表几何正确"的落地。
8. 发射顺序：`walls_first` 时 墙（外→内）→ 延伸墙弧 → 填充；否则 填充 → 墙（内→外）。

### 3.6 缝隙填充残段的局部拜访

`splice_surface_fragments()`（`ContinuousPrint.cpp:398`）处理顶/底面里的 gap-fill 小段与短实心残段：把每条残段"插入"到最近的宿主填充线的中点位置（用 `split_open()` 在宿主上切开，串入 `连接段 + 残段 + 连接段`，合成 `ExtrusionMultiPath`）。每条连接的端点距离都受 `max_join` 约束，残段自身的 role/宽度/流量保留。另有针对**稀疏→实心过渡层**的近闭合轮廓补缝、以及长度 > 上限的短曲线残段按弧长二分后局部接入的处理。

### 3.7 发射与层间衔接（固定层高版本）

- `emit_continuous_print_layer()`：按 `plan.order` 依次 `extrude_path` / `extrude_multi_path`；因为相邻实体首尾精确相接，`_extrude()` 起手处发现喷嘴已在段首，`travel_to` / `needs_retraction` 没有任何可触发点 → 整层零空驶。发射期间关闭圆弧拟合，保证连接顶点是显式 G1 顶点。
- **层间**：`m_continuous_prev_end` 保存上一层链尾，作为下一层 `preferred_start`；换层在上一层终点原地 `travel_to_z` 抬一个层高（XY 不变）。
  - 若下一层首段起点与上一层终点距离 ≤ `max_join`，直接在计划头部插入一段短连接；
  - 距离更远时，改用 `build_contour_connected_chain(connect_previous=true)`，沿轮廓接入并做局部避让；
  - 若该层最终回退常规发射，清除 `m_continuous_has_prev`（否则下一层会从一个**并不真实**的终点开始规划）。
- 计划中的 `total_length` / `sampling`（1mm 弧长插值采样）字段被保留，用于将来的离体/穿模检查，当前版本不消费。

### 3.8 配置、界面与用户提示

| 项                                     | 内容                                                                                                                                    |
| -------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| `continuous_print_mode`              | bool，默认 false，comAdvanced；Print 设置页「Special mode」                                                                             |
| `continuous_print_max_join_distance` | float，默认 2.0mm，范围 0–10，0 = 严格不补线                                                                                           |
| 互斥                                   | 与`spiral_mode` 互斥（spiral 优先）；`spiral_mode_smooth` / `spiral_mode_max_xy_smoothing` 不再被连续打印复用（旧螺旋方案已移除） |
| 自动禁用                               | 开启时强制关闭`retract_when_changing_layer` / `filament_retract_when_changing_layer`                                                |
| 提示                                   | `GCodeProcessorResult::continuous_print_report` → GUI 通知 + CLI stderr + 日志；结构门不通过 / 无层可一画 / 部分层回退三种文案       |
| 诊断                                   | `CP_DEBUG=1` 环境变量打印每层实体清单与失败 hop                                                                                       |

### 3.9 验证工具

- `tools/continuous_print_check.py`：解析 G-code，逐层统计**层内空驶**（两条挤出之间的非挤出 XY 移动）、**断链**（相邻挤出首尾不重合）、**层界空驶与间隙**、回抽、Z 回退、层内 Z 是否平整；`--allow-z-ramp` 仅用于审计历史螺旋输出；违规即退出码 1。支持模态角色、`G92`、绝对/相对 XYZ 与 E、`G2/G3`。
- `tests/continuous_print_check_tests.py`：对上述解析器的 5 个用例（模态角色与首层跳过、Z-ramp 拒绝/允许、`G92` 与圆弧、越界返回仍算空驶、相对 XYZ 结合 `G92`）。
- `tests/libslic3r/test_continuous_print.cpp`：8 个 `TEST_CASE`（精确链化各分支、重排缝保持墙角、表面碎段局部拜访、曲线残段与裁缝环、可达碎段直连、实心面在当前位置进入并预留轮廓延伸、越过最近填充段的轮廓布线、稀疏层从真实实心终点沿轮廓进入、短层界接入不扭曲首段）。

---

## 4. 已实现功能

1. **层内一笔画发射**：开启 `continuous_print_mode` 后，可一笔画的层被组织成**单条连续挤出链**并整层发射，层内**无任何非挤出 XY 移动**。
2. **闭合环 + 开放链统一处理**：`ExtrusionLoop` 被线性化为开放 `ExtrusionPath`（可在指定点置缝），链尾可为开放端点；层起点按上一层终点（转移点）选择，因此层间也是同一套机制。
3. **多墙一笔画**：墙按面积降序由外向内，与填充之间通过"填充入口 → 强制置缝传播 → 逐圈墙"或反向路径串成一笔；实测逐层交替 `外墙→内墙→填充` / `填充→内墙→外墙`。
4. **顶/底面与连续填充接入**：`monotonic`（顶/底/内部实心）与 `alignedrectilinear`（稀疏）图案可被纳入；墙-填充通过"不完整墙壁"切向延伸衔接。
5. **实心层沿轮廓连接**：用最内墙的内偏移轮廓生成额外墙弧接入填充，并按线宽预留局部避让带、裁剪填充、沿边界接回，逐段做几何与连续性校验——不再出现跨越实心区域的长斜线。
6. **有界连接**：墙↔墙、墙↔填充/支撑的短连接由 `continuous_print_max_join_distance` 限制，0 时退化为"严格不补线"（走欧拉迹判定 + 接合点拆分路径）。
7. **固定层高**：每层固定 Z，换层在上一层终点原地抬升一个层高，不再有 Z-ramp、渐入减流或平滑插值。
8. **逐层回退与显式提示**：结构门不通过、单层不可一笔画时自动回退常规打印，并在 GUI/CLI/日志明确告知（应用层数 N/M），不再静默。
9. **零回归保证**：新选项默认 false；关闭时输出与基线一致（清单记录为"除生成时间外逐行一致"）。
10. **可自动化的验收工具**：C++ 专项单测 + Python G-code 审计器 + Python 审计器单测。

**实测覆盖（来自清单记录，均为几何/G-code 级别，未做实机打印）**

| 模型                              | 结果                                                        |
| --------------------------------- | ----------------------------------------------------------- |
| 圆柱体（3 圈墙，130 层）          | 130/130 层固定 Z、零层内与层间 XY 空驶、零 Z 回退           |
| 六棱柱（75 层）                   | 75/75 层通过；第 5–69 层 XY/E/角色与用户原始文件逐段一致   |
| 立方体 1/2/3/4/6 圈墙（各 50 层） | 49/49/50/50/49 层零层内空驶；1/2/6 圈墙的既有第 47 层仍回退 |
| 关闭功能                          | 与基线逐行一致（仅生成时间不同）                            |

---

## 5. 重难点

### 5.1 目标本身的内在矛盾（数学层）

"零空驶 + 不补线"要求层的挤出图存在欧拉迹，而真实切片输出（平行填充线、多道互不共点的墙）的奇度顶点远多于 2。**这不是算法没写好，而是目标与几何的矛盾**——经典解法（中国邮路：给奇点补重复边）恰好被约束禁止。整条技术路线的合理性都建立在"先严格判断可行性、再对可行层做处理、不可行就回退"上，而不是"把任意模型硬掰成一笔画"。

### 5.2 从坐标相等改为"物理容差 + 吸附"（最关键的一次修复）

M1/M2 用 `SCALED_EPSILON`（≈1e-4mm）判定端点接合，理论上正确，但**真实切片中墙与填充之间有 `infill_wall_overlap` 级别的缝**（顶/底面 ≈0.12mm，内部实心 ≈0.48mm）。结果是整个特性"看起来接好了、实际一次都没命中"，Z-ramp 行数为 0。

修法是双管齐下：容差改成物理量（0.5 × 喷嘴直径），并把容差内的接触端点**吸附**到接合点（位移 ≤ 容差、**不新增任何线段**，因此仍满足"不补线"）。这个"容差取物理量还是几何量"的判断，以及"吸附而非补线"的取舍，是整条路径上最容易走错的地方。

### 5.3 接缝必须落在线段中间（插顶点），不能只吸附到已有顶点

多墙场景长期是 0/49。根因是墙环置缝只允许落在**已有多边形的顶点**上，而墙-填充缝 0.12mm 恰好落在两个顶点之间，永远置不上精确的落点。改为在**边上插顶点**（`reseam_closed()`）后立刻变成 42/49。同类问题在 `rotate_ring()`（闭环旋转线性化）里也必须处理，而且早期版本还犯过"旋转时漏掉原始顶点"的错（导致墙角与周长丢失），被单测"重排缝保持每个墙角"挡住。

### 5.4 拆分还是保留整环：两个模式的不同策略

接合点拆分（lollipop）在"严格不补线"下是必要的，但**在有界连接模式下反而是有害的**：墙环若在多个填充端点处被拆开，就变成一堆互不相关的开放片段，按面积排序的"外→内"顺序随之失效，多墙直接回退。最终方案是分模式处理：`max_join > 0` 时保留完整墙环走贪心排序，`max_join == 0` 时才走拆分 + 欧拉迹判定。这类"某个机制在 A 模式是解药、在 B 模式是毒药"的取舍，需要按模式分化而不是统一实现。

### 5.5 零空驶 ≠ 几何正确（用户提出的关键约束）

第一轮"零空驶"通过后仍然存在**从外墙直连填充入口的长斜线**：XY 移动确实带挤出、计数为 0 空驶，但这条线跨越了实心区域，是悬空/多料。因此验收必须同时检查：沿墙形状、填充避让、层界起点、实际无挤出移动。这直接催生了 3.5 节的整套"沿轮廓延伸墙 + 局部避让带 + 裁剪填充 + 沿边界接回 + 逐段几何/连续性校验"，一次失败的校验就整层回退。

### 5.6 层间方案被用户要求推翻并重做

设计文档（09-07）的层间方案继承 SpiralVase：Z-ramp 渐进升 Z、XY 平滑滑移、travel 过滤焊接。用户随后明确"**每层固定 Z，换层保持上一层终点的 XY、只抬升一个层高**"。这带来一串连锁修改：删除螺旋过滤器与 `LayerResult::continuous_print_enable`、移除 `spiral_mode_smooth` 预算校验与 GUI 联动、`change_layer`/`lazy_lift` 改走普通 Z 提升、审计工具从"按 Z-ramp 选连续区"改为"检查全部模型层 + 固定 Z"。**同时被推翻的还有"为保留旧输出而在实心层补收尾"**——层计划终点必须是真实最后一笔的终点，否则下一层会从一个不真实的坐标开始规划。

### 5.7 层界起点的"真实性"与回退状态清理

层界衔接依赖 `m_continuous_prev_end`。若某层判定失败回退常规发射，必须**清除**这个终点（改用实际喷嘴位置），否则下一层会以错误的 `preferred_start` 规划；反之，短连接与沿轮廓接入又必须提前进入下一层的几何计划。这两条互相牵制，属于典型的"状态在过滤器/回退路径之间的传递正确性"问题。

### 5.8 复杂几何的局部处理

- **入口候选**：最近的填充段可能位于一条可连通链的**中部**，以它为入口会误判为不可连通；需要按距离尝试其他端点/中点作为种子，再沿墙弧接入（否则多墙实心层随机回退）。
- **避让带的多连通**：对尖锐轮廓做偏移会产生微型空袋；只允许丢弃**不含任何填充**的空袋，一旦剩余区域不止一个或带孔就必须拒绝，否则连接会跨区。
- **尖角、窄折返、残段**：近闭合轮廓的补缝只对含稀疏填充的层启用（否则会把普通实心层里的窄折返误当环）；长度超上限的短曲线残段要按弧长二分后局部接入，同时保持每段的原始 role/宽度/流量。

### 5.9 与既有机制的交互

- 连续打印与 `spiral_mode` 必须互斥，且过滤器槽位（`has_vase_filter`）要正确分派，避免两套判定打架。
- `change_layer` 的 Z 提升、`lazy_lift`、`retract_when_changing_layer`（开启时在 `normalize_fdm` 里强制关闭，避免出现未配对的回填）、`timelapse` 插码条件、圆弧拟合开关，都需要在连续模式下单独考虑。
- **新增打印选项必须同时登记进 `Preset::print_options()`**：只加 def + GUI 行会因设置页读取到 null 而在「Others」页崩溃（已加回归单测守卫）。这条经验对今后任何新增打印选项都适用。

### 5.10 性能与规模化

- 端点合并是 O(n²)，弧长/最近点查询逐线段扫描；每层做一次 preflight，实心层还涉及多次 clone、`offset`、`diff_ex` 与逐段包含性校验。
- 排序失败时的**二次/多次尝试**（原始顺序 → splice 后 → 多组种子）提升了成功率，但也是有意的成本换正确性。当前只对薄壁/壁-填充量级的目标模型成立；上千段填充的层本来就会被奇度判定拒绝。

### 5.11 验证本身的难度

- 合成单元测试会误导（"理论上端点重合"与真实切片输出差 0.12mm），所以必须用真实 CLI 切片 + G-code 解析审计，并把**几何合法性**（避让距离、包含性、连续性）与**空驶计数**分开断言。
- 回归不能只做"差异存在与否"，清单里给出的强证据是"**关闭功能时逐行一致**"和"**第 4–125 层逐段 XY/Z/E/角色不变**"这类逐段对比，这比抽样目视可靠得多。
- 审计工具自身也需要单测（`tests/continuous_print_check_tests.py`），否则解析器的 bug 会被误读成切片器的 bug。

### 5.12 工程环境坑（清单记录）

- 全并行 `-m` 构建会编译器堆耗尽，需 `/maxcpucount:2`。
- 首次配置需 `cmake .. -DBUILD_TESTS=ON` 才有测试目标；`RelWithDebInfo` 产物在本机无法运行（无输出直接退出），验证需用 `build/` 的 Release 构建配合 CLI。
- 全量 `libslic3r` 测试始终有 1 个失败：`Placeholder parser coFloatsOrPercents vector access` SEGFAULT——**基线上即存在**，与本功能无关，不应被当作本次改动引入的回归。

---

## 6. 已知限制与未完成项

| 项                         | 状态                                                                                                                                                   |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 内部实心填充层             | 部分几何仍回退：填充在`by_region` 里可能是多个实体，彼此相距可达 24mm（真实几何间隔，非排序问题），补连接不可接受                                    |
| 支撑层、部分过渡层、多岛层 | 回退常规打印（跨空气连接无条件禁止）                                                                                                                   |
| 跨墙/跨填充的适用性        | 仅"合适模型"：单对象单实例、单材料/单喷嘴、`ByLayer`、无擦料塔；层内需可一笔画                                                                       |
| 离体检查（穿模/悬空）      | **未实现**：`ContinuousLayerPlan::total_length` 与 `sampling` 字段已备好（1mm 弧长插值采样），但尚未有消费方；需把层多边形或网格体积引用传入 |
| 实机打印验证               | **未做**：所有结论均来自几何、切片与 G-code 审计；层界焊缝、强度、挤出一致性、转移点附近是否积料/凹陷均未实测                                    |
| 圆弧拟合                   | 连续发射期间显式关闭（保证连接顶点可见与审计一致性），影响该模式的输出体积/平滑度                                                                      |
| 遗留代码                   | `SpiralVase.hpp` 中 `SpiralVaseHelpers` 的注释仍写着"供 ContinuousPrint 过滤器共用"，但该过滤器已在 `cd328070cb` 删除，注释未同步                |
| 开放问题 O1–O4            | 逐层混合过渡策略、"环间贴合打印"、填充内补线的有界放松等，v1 不实现（见设计文档第 7 章）                                                               |

---

## 7. 附录：文件与命令

### 7.1 功能改动文件（`8500fcdcca..HEAD`）

| 文件                                                                                     | 性质     | 内容                                                                                    |
| ---------------------------------------------------------------------------------------- | -------- | --------------------------------------------------------------------------------------- |
| `src/libslic3r/GCode/ContinuousPrint.hpp/.cpp`                                         | 新增     | 判定器`preflight_layer`、接合点拆分、贪心单链排序、实心层沿轮廓链、碎段拜访、发射计划 |
| `src/libslic3r/ShortestPath.hpp/.cpp`                                                  | 新增函数 | `chain_extrusion_entities_exact()`（欧拉迹 + Hierholzer）                             |
| `src/libslic3r/GCode.cpp/.hpp`                                                         | 修改     | 结构门、逐层 preflight 与单链发射分支、固定 Z 层变、报告、圆弧拟合/回抽处理             |
| `src/libslic3r/PrintConfig.cpp/.hpp`、`Preset.cpp`                                   | 修改     | 两个新选项、选项登记、开启时禁用换层回抽                                                |
| `src/libslic3r/GCode/GCodeProcessor.hpp`                                               | 修改     | `continuous_print_report` 字段与拷贝                                                  |
| `src/libslic3r/GCode/SpiralVase.hpp/.cpp`                                              | 修改     | `SpiralVaseHelpers` 迁移为 inline（原为供两个过滤器共用；过滤器已移除）               |
| `src/slic3r/GUI/{Tab,Plater,ConfigManipulation}.cpp`                                   | 修改     | 设置页两项、切片完成通知、平滑选项可见性回归原状                                        |
| `tests/libslic3r/test_continuous_print.cpp`、`tests/continuous_print_check_tests.py` | 新增     | C++ 专项测试、审计器单测                                                                |
| `tools/continuous_print_check.py`                                                      | 新增     | G-code 零空驶/单链/固定 Z 审计                                                          |

### 7.2 常用命令

```bash
# 专项 C++ 测试
ctest -C Release --test-dir build/tests/libslic3r --output-on-failure
build/tests/libslic3r/libslic3r_tests.exe "[ContinuousPrint]"

# 审计器单测
python -m unittest tests.continuous_print_check_tests

# CLI 切片并审计（--slice 必须带板号）
build/src/Release/orca-slicer.exe --slice 0 --outputdir <dir> \
  --load-settings "<machine.json>;<process.json>" --load-filaments "<filament.json>" <model.stl>
python tools/continuous_print_check.py <dir>/plate_1.gcode

# 排序诊断
CP_DEBUG=1 ...
```

### 7.3 关键行号索引（当前 HEAD）

| 主题                     | 位置                                                |
| ------------------------ | --------------------------------------------------- |
| 欧拉迹精确链化           | `ShortestPath.cpp:1078`                           |
| 接合点拆分               | `GCode/ContinuousPrint.cpp:275`                   |
| 贪心单链排序（有界连接） | `GCode/ContinuousPrint.cpp:515`                   |
| 严格模式下 loop 线性化   | `GCode/ContinuousPrint.cpp:244`                   |
| 碎段局部拜访             | `GCode/ContinuousPrint.cpp:398`                   |
| 最短弧段 / 沿边界裁剪    | `GCode/ContinuousPrint.cpp:773` / `:813`        |
| 实心层沿轮廓链           | `GCode/ContinuousPrint.cpp:868`                   |
| 判定器入口               | `GCode/ContinuousPrint.cpp:1049`                  |
| 结构门                   | `GCode.cpp:5648`                                  |
| 单链发射                 | `GCode.cpp:5665`                                  |
| 逐层 preflight 分支      | `GCode.cpp:5475`                                  |
| 结构门与报告初始化       | `GCode.cpp:2564`（报告组装在 `GCode.cpp:2144`） |
| 审计工具                 | `tools/continuous_print_check.py`                 |
