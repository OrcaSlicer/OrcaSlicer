# 用户需求与实际执行对照

本文件用于恢复会话上下文和核对交付。它不是新增实施授权，也不把历史计划当成当前任务。

## 上一条实施要求原文

> 这个协同任务我交给另一个会话处理了；跟你还是做项目问题，我发现了几个体验上的问题，你分析优化下：
> 1、这个棋盘格就默认去掉，不需要还增加个选项，或者担心有物体颜色跟背景颜色一样看不清，你也做好棋盘格不带入到图片里，只是展示；
> 2、这个压缩到6个颜色，看起来应该不止6个颜色，你看看是这样吗，为啥？
> 3、高级工具这里都不要了，有啥需要修改的都到3D美颜里做就行了，如果想要重新设计，就在重新开始那里做，让用户确认是否要检查一下原因给一些建议再去重新设计
> 4、3D美颜这个功能一定是要的，你帮我好好打磨，多找一些开源的办法或者工具，把这块好好设计一下；
> 5、导入到准备也这里的颜色匹配，你先帮我把这块的逻辑搞清楚，然后看看这里怎么优化，现在颜色匹配很乱，你看下这里怎么跟我之前的逻辑配合，以及这部分如何优化，也大量找找开源方案；
> 以上你列一个计划，然后定个目标就开始干，中间不用问我，就按你的推理执行。

## 实际执行与边界

| 要求 | 已有代码和验证 | 不能扩大解释的部分 |
|---|---|---|
| 默认去棋盘，无额外选项 | 移除原始AI图开关；透明画布用灰底；现有历史展示副本默认使用；白色透明测试图与用户人物图主窗口验证 | 没有可靠遮罩或展示副本的旧RGB棋盘图尚不能保证无损去除；不是对所有历史图片都已根治 |
| 六色可信 | 默认无光照平涂，光照立体可切换；区分原始RGB数、基础色数、耗材槽数；主窗口切换验证 | 显示与打印有差别；GPU片元与导入面采样边界并非逐像素完全相同 |
| 工具集中／重新设计 | 移除高级工具和重复重新设计区；编辑集中到美颜工作台；重新开始有直接、建议、取消；取消保留作品，直接开始复用参考图 | 有已有报告时复用报告；无报告时是明确标注的一般建议，没有实现新的按模型自动诊断服务 |
| 美颜打磨／开源研究 | 整体／局部柔化、网格修复、局部改色、前后对照、版本回退／重做；局部三圈边界渐变；参考Open3D、libigl、CGAL等；实际人物局部处理12.02秒到6.99秒 | 这是基于现有能力打磨的一版工作台，不是已完成完整的美图式雕刻、语义五官编辑或实物效果验收；42%是局部文件处理，不是云端生成提速 |
| 试色到导入配色 | 默认完整颜色匹配链路传递固定分组中心和目标色；六目标匹配已有耗材；原生窗口分开原色／目标／耗材视图；实际导入成功、4槽不变、不自动切片；普通Ctrl+I单独回归 | 旧AutoMap／ManualMatch路径未全面统一；CMYW／RYBW配方不是CMYK标定；没有实际CMYK打印证据 |

## 验证证据

- 计划：`Docs/plans/2026-09-09-workbench-color-experience-execution.md`。
- 实施／开源／验收报告：`Docs/audits/2026-09-09-workbench-color-experience.md`。
- 原生配色研究：`Docs/audits/2026-09-09-native-color-import-chain.md`。
- Windows构建通过；组合测试53例4719断言，原生分色9例902断言，背景2例6子场景。
- `.tmp/ux-performance-20260909/final-six-color-import.3mf`及同目录截图记录实际主窗口验证。
- 架构检查仍有4项既有问题：两处测试凭据字面量、两条历史来源ref缺失，不属于全部检查通过。

## 会话显示不一致

用户反馈“我的输入又不见了”后，2026-09-09再次通过会话读取工具检查，返回的最近可读记录仍显示早期智能切片交互修复和首次编译过程，与本轮上下文中的后续五项需求及工作区现有成果不一致。只能确认记录视图不一致，不能断言消息被删除或确定客户端故障原因。本文件保留当前上下文仍可见的需求原文，防止再次丢失对照依据。

上一轮“本轮完成”应理解为这一版实施和列出的验证完成，不能表示上述所有边界都已经解决。
