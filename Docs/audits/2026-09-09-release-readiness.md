# 当前版本发布验收

日期：2026-09-09，收尾时间 10:14（Asia/Shanghai）。用户要求完整测试当前版本并判断能否发布。当前环境可执行的全量自动测试、Release 构建和静态审查已完成；实际 GUI、供应商、安装包及真机验收的缺口保留为未验证。

## 测试对象

仓库 `D:/Workspace/11_3DDY_Continue`，分支 `codex/continue`，HEAD `ad4f74b97ae89d3cd9371d0c3214cd31f3b6269f` **加当前未提交工作树**。不能仅用这个提交号表示本次已测试源码。开始检查时记录了 63 条工作树状态和 60 个变更源码/文档的哈希，见 [source-identity.json](../../.tmp/release-readiness-20260909/source-identity.json)。临时输出、图谱和生成模型另作资源范围检查。

## 当前判断

**尚不具备直接发布条件，也不建议将当前工作树直接交给外部测试者。** 三维修整的版本保存、返回原版本和预览清理存在三个具体问题；此外 Python 自动测试未全绿、集成检查失败、发布脚本仍绑定旧分支，没有干净发布源码及最终安装器/便携包，实际主窗口验收也缺失。

六套原生测试合计 **730 个用例**：标准环境 **725 通过、5 跳过、0 失败**；仅对跳过用例补充 NumPy 环境后 **5/5 通过**。Release 主程序与六套测试目标均构建成功。系统和随包 Python 各执行 **652 个测试方法，均有相同的 8 个方法失败**，这两个运行不是 1304 个不同用例。

## 验证矩阵

| 范围 | 当前结果 | 证据与限制 |
| --- | --- | --- |
| 架构/集成 | 失败，4 项 | 2 项测试夹具字面量凭据扫描发现，2 项旧功能分支来源 ref 缺失；保留全部检查，未跳过 Git。 |
| 系统 Python AI 全套 | 652 项执行，6 条失败、11 条错误、0 跳过 | 失败/错误包含参数化子用例，归属 8 个测试方法；初步分类为旧流程预期和测试模拟未同步。 |
| 程序携带的 Python AI 全套 | 同样 652 项，8 个失败方法，0 跳过 | Python 3.12.13 / Pillow 12.2.0；与系统运行完全相同的失败标识和测试文件哈希。8 个方法隔离重跑均复现。每套其余 644 个方法无失败/错误。 |
| Release 主程序与全部 C++ 测试构建 | 通过，退出 0 | 09:37:55 至 10:12:39，主程序及六套测试目标均完成；构建日志保留，存在非致命的测试类型比较和运行库链接警告。 |
| slic3rutils | 219 通过，5 因缺少 NumPy 跳过；补充 5/5 通过 | 标准测试环境结果保留；补充测试仅通过当前进程 PYTHONPATH 使用已安装 NumPy，未修改产品运行时。包含 AI、智能切片、修整、原生准备、插件等，共覆盖 224 个用例。 |
| libnest2d | 21/21 通过 | 原生排版和几何用例。 |
| libslic3r | 316/316 通过 | 当前工作树重新构建并运行全部切片核心测试，23.2 秒。 |
| fff_print | 145/145 通过 | 当前工作树重新构建，完整 FFF 流程测试 33.17 秒；覆盖填充、支撑、混合耗材、擦料塔和 G-code 等。 |
| sla_print | 21/21 通过 | 支撑生成、射线检测及栅格输出等，23.75 秒。 |
| filament_group | 3/3 通过 | 黄金结果、默认预算与属性检查，50.57 秒。 |
| 隔离 Python/Pillow 运行时 | 通过 | Python/Pillow 版本、模块位于 bundled runtime 内，以及原生 PNG 编解码往返。 |
| 包扫描器自身 | 8/8 通过 | 仅证明扫描器测试通过，不是当前发布包通过。 |
| 实际安装器和便携 ZIP | 未执行，无本版产物 | 当前只有开发运行目录和应用 EXE/DLL，没有最终成对包和绑定哈希的扫描报告。 |
| 主窗口实际旅程 | 未完成 | 前次使用独立数据目录和本地模拟端点启动预览 Orca，被自动审批以 `blocked by policy` 拒绝，未提供更具体原因；限制未解除，本次未通过其他入口重试。当前未发现可接管的运行实例。 |
| 真实供应商 / 真机打印 / macOS / Linux | 未验证 | 本轮不调用收费服务；当前环境是 Windows，无真实打印或其他平台运行证据。 |
| git diff --check | 通过 | 仅有现有 LF/CRLF 配置提示。 |

## 已确认的遗留项

### 三维修整的产品缺陷

以下结论来自当前代码的数据流审查，**尚未在 GUI 实际操作中复现**。现有 6 个修整几何单元测试不覆盖这些模型库和界面状态事务，不能因它们通过而忽略以下路径。

| 优先级 | 触发与实际后果 | 代码证据 / 修复验收 |
| --- | --- | --- |
| P1 | 打开 `generated_models/<job>/model-vertex-color.obj`，修整并接受：新 OBJ 留在同一任务目录，模型库却只枚举固定原文件名。已接受版本不能重新选取；删除原记录会递归删除目录，连带删掉已接受版本。 | `src/slic3r/GUI/AI/ModelGeneration/ModelGenerationFinishingView.cpp:145,264`；`src/slic3r/GUI/ModelGenerationPanel.cpp:4228,4751`。需要正确登记/发现派生版本，并限定删除所有权；覆盖接受、刷新、重启和删除原记录后派生版本仍可用。 |
| P2 | 带有任务、颜色方案、颜色意图及审核结果的原模型，接受修整后选择“返回上个版本”：只恢复路径，原任务 ID、颜色和审核上下文被清空；再次导入不再携带原来的信息。 | `ModelGenerationFinishingView.cpp:219-231,289`；`ModelGenerationPanel.cpp:2648-2666`；`ModelGenerationPresentation.cpp:154-159`。应恢复原模型完整上下文，校验返回前后的导入请求和审核门槛一致。 |
| P2 | downloads 中的修整预览尚未接受，删除另一条模型记录或提交其他记录的打印反馈触发刷新：预览被当正式模型列出。选中它后，失效清理把当前选中的 OBJ 删除，界面仍留着内存预览，导入文件已不存在。 | `ModelGenerationFinishingView.cpp:91-96`；`ModelGenerationPanel.cpp:4242,4493,4590,4618,4718,4820`。未接受的临时输出应排除出模型库，清理不能删除当前所选文件；覆盖等待接受期间的刷新和切换。 |

详见 [修整审查记录](../../.tmp/release-readiness-20260909/finishing-release-review.md)。三项均应在本版发布前修复并验证；本轮未修改生产代码。

### 发布前必须处理

1. **自动化回归仍红。** 更新与当前产品行为不一致的测试预期、旧模拟函数签名和缺失的提供商可用性夹具，然后重跑相关测试及全套回归。不能通过删除断言或依赖本机真实凭据消除失败。现有证据尚未独立确认这些失败代表新的产品缺陷，诊断详见 Python 报告。
2. **集成检查 4 项失败。** `release/test_verify_package_contents.py:39`、`tools/ai/test_diagnostic_failure_flow.py:106` 被凭据扫描识别；`codex/model-generation` 和 `codex/smart-slicing` 本地来源 ref 缺失。前两项位于测试夹具，不据此声称泄露了真实凭据；后两项反映续建仓库与旧验收元数据不一致。需要合规修正夹具/续建来源验收，不能造 ref、跳过检查或重试仍被阻塞的智能切片来源比较。
3. **发布流程尚未迁移到续建仓库。** `release/build_internal.ps1` 与 `scripts/package_internal_fast.ps1` 仍要求旧 `codex/orca-integration-v2` 分支。应适配当前确定的发布来源并保留干净源码、准确版本和包检查要求，而非切回旧工作区执行。
4. **源码与版本尚未封版。** 当前有未提交源码及新增测试；构建元数据仍是 `2.5.0-dev`、`internal`、`ux-fixes-20260908`。必须先确定本版范围、版本和可追溯源码，再产生实际交付物。
5. **最终包和安装验证缺失。** 默认发现位置未找到 NSIS（尚不等于整台机器没有其他安装）；没有当前版本安装器及便携 ZIP。需要完成构建、包扫描、安装/卸载、便携启动和升级/旧 3MF/profile 验证。
6. **主窗口体验未验收。** 风格入口、连续颜色、三维修整的比较/接受/放弃、页面切换状态、历史恢复、原生配色导入、失败恢复、手动切片、主题和 DPI 均缺少当前版本实际操作证据。已有 HTTP、几何和 C++ 通过结果不能替代。

### 打包范围与工具问题

- `resources/generated_models/` 中有 6 个未跟踪的微型本地重着色测试 OBJ。当前 CMake 安装规则复制整体 resources 树；封版前明确排除这些实验输出，避免把它们当正式资源。没有证据表明这些文件含私人照片或凭据。
- `release/build_internal.ps1 -ValidateOnly` 提前返回，未执行 README 声称的凭据缓存拒绝检查。实际 build/fast-package/CMake 仍有防护；这是校验入口与说明不一致，不能据此声称真实包带有凭据。
- 当前缓存 `ORCA_AI_INTERNAL_DEFAULTS_FILE` 为空。配置检查通过不能替代最终 EXE/ZIP 的内容扫描。
- 原生测试构建有 `C4805` 类型比较和 `LNK4098` 运行库冲突警告，构建与测试均通过；未通过屏蔽警告改变结果，不能将本轮称为零警告构建。

### 后续能力与证据边界

- AI 增强 Orca 原生颜色匹配仍是交给同事的第三项，本次不把它算作已纳入的能力。
- 当前本地三维修整是受控表面平滑和局部网格清理，尚无语义五官重塑、自动补洞、纹理接缝修复或色斑自动处理；大模型实际交互耗时/内存未验收。
- 第二家真实 3D 提供商、真实生成稳定性，以及六色 CMYK 视觉叠色的真机/实物效果没有本版验收证据。这些目标应与内部测试版实际承诺区分，不能借发布测试版宣称全部产品目标完成。

## 证据索引

证据目录：[.tmp/release-readiness-20260909](../../.tmp/release-readiness-20260909)。包括集成 JSON、各套测试日志/JUnit、源码身份、运行时校验、[Python 失败诊断](../../.tmp/release-readiness-20260909/python-release-readiness.md) 与 [打包审计](../../.tmp/release-readiness-20260909/package-audit.md)。本轮只测试和评估，没有发布、推送、联系测试者、修改生产源码或清理旧工作区。

Python 执行范围是 `tools/ai/test_*.py` 的全部 45 个文件，另运行 release 包扫描器的 8 个用例。`scripts/test_moonraker_lane_data.py` 是面向真实打印机的数据库修改/删除脚本，未作为单元测试执行。两套 Python 全量测试采用进程内离线保护，不依赖真实供应商凭据；C++ Http 夹具包含公共测试端点，因此不能将整轮 C++ 测试称为完全离线。未调用收费生成服务。

收尾校验：[source-unchanged.json](../../.tmp/release-readiness-20260909/source-unchanged.json) 确认开始时记录的 60 个变更源码/文档哈希没有变化；[final-binary-hashes.json](../../.tmp/release-readiness-20260909/final-binary-hashes.json) 保存主程序及六套原生测试程序哈希。先完成的工具类/排版测试及主程序在后续构建后哈希保持一致，无需为不变二进制重复运行。原生汇总见 [cpp-test-summary.json](../../.tmp/release-readiness-20260909/cpp-test-summary.json)，各套详细耗时以日志为准，JUnit 时长字段取整。

下一版建议按顺序完成：修复三个修整事务问题并补充针对性回归；修正 Python 旧预期及夹具、解决四项集成发现；适配发布脚本和封版资源；完成主窗口实际旅程；最后生成并扫描 EXE/ZIP，完成安装及便携验证后再判断是否适合对外测试。

## 11:05 启动复核补充

用户明确要求重新打开主窗口后，使用当前 `build/ux-preview/orca-slicer.exe` 和新的独立数据目录启动成功，本次没有收到平台拒绝。进程 56792 的窗口标题为“未命名 - OrcaSlicer”，有有效窗口句柄且 `Responding=True`；见 [窗口确认](../../.tmp/main-window-launch-20260909/window-confirmation.json)。因此此前启动受阻的状态已解除，但此前拒绝未给出具体原因，本次成功也不能解释其原因。这里只确认主窗口启动，不等同于完整 GUI 旅程验收，其他发布结论不变。

## 后续图片确认实测与修复

用户随后报告完整白衣人像被误判碎片，已复现并修复 RGB 颜色分割被当作主体断裂的错误。新版已在同一实际任务验证重启恢复、生成按钮启用、明确风险提醒，以及创作/准备/原生预览切换后的状态保留。详细结果见 [图片预检修复报告](2026-09-09-reference-preflight-fix.md)。后续全量 Python 656 项仍为同样的 8 个失败方法，C++ 工具/AI 224 项通过；其余发布阻塞仍然存在，上表是修复前基线，不应当成修复后的完整端到端验收。
