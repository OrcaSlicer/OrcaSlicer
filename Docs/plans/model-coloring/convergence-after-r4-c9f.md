# R4-c9l 起的定序收敛计划

## 当前基线

- 分支：`codex/feat/local-semantic-coloring`
- 用户程序：`build-semantic/runtime-r4-c9l`
- 启动入口：`.tmp/semantic-validation/gui-rounds/r4-c9l-user/Start-R4-c9l-test.cmd`
- `OrcaSlicer.dll` SHA-256：`6076020f332982d0d0247d8cd3d80954b644d56f478e6013dcce7877cc6b0ed3`
- 源码检查点：`.tmp/model-coloring-checkpoints/COL-005-20260917-045503-926733-r4-c9l-ear-spatial-built`
- c9l 相对 c9f 只增加一次、不可级联的三维耳肤桥接。用户指出的刘亦菲上耳缘暖肤色误发色像素从 39 降至 15，大框从 67 降至 27；固定区域、八眼和动物没有新增变化。
- 用户六视角确认 c9l 的整体结果可用，但刘亦菲左耳上缘仍与头发串色，因此不能命名为 `ear-hair-v1`。c9i／c9j／c9k 已证明共享边扩展和适配器单像素闭合触达不了极侧视角的网格断缝。
- c9f 的眼部结果仍是当前眼部起点：八眼核心缺 24 像素；方飞左眼、方飞右眼、双人女性右眼低于 98%。c9g、c9h 已证明继续扩大材料补边收益过低。

用户只需提供四模型六视角图片和自然语言描述。Codex 负责版本身份、截图归档、区域定位、面／子面诊断、自动回归、构建和回溯记录。

## 固定顺序

后续严格按以下顺序推进。一个阶段未通过时不进入下一阶段；失败候选保留证据并从生产代码撤回。

### S0：刘亦菲左耳与头发边界冻结

**输入**：用户对 c9l 的刘亦菲六视角反馈。方飞耳鼻、双人耳发和动物由同一版本的六视角一并作为保护检查。

**处理**：

1. 若用户确认通过，冻结 c9l 的六视角输出和差分为 `ear-hair-v1`，不再修改耳发规则。
2. 若仍有串色，将残留截图映射到原面 ID、二级子面路径、原色、法线和最近直接耳肤证据。
3. 只允许一个 c9m 假设：处理 c9l 剩余的低置信／Unknown 根面，同时要求已有深发色材料、空间邻近、同向和固有肤色兼容；不得级联，也不得修改识别适配器。
4. c9m 若不能显著减少用户指出的残留，或改变任一保护区，立即撤回并把剩余项登记为遮挡／拓扑不确定带，不继续扩大半径或传播层数。

**c9m 结论**：已验证并撤回。第一版允许所有低置信／Unknown 深发色根面时范围过宽；收紧为模型对角线 `0.2%` 和法线点积 `0.94` 后，目标耳框仍为 27 个可疑像素不变，却在方飞／刘亦菲／双人增加 126／164／844 个叶。该假设无目标收益，未进入用户程序。刘亦菲左耳残留登记为 `R4-C5/c11c` 固定反例，后续用材料边界子面证据处理；不能标记为通过。

**自动否决**：眼白、虹膜、眉毛、嘴唇、衣料、31 个历史 ROI、13 个原生 4 倍 ROI、9 个侧背区或动物出现新增变化。

**完成条件**：刘亦菲确定耳肤不再使用头发材料；真实发丝和耳前遮挡保留；方飞耳鼻不新增红色；用户六视角确认通过。通过后冻结为 `ear-hair-v1`，后续轮次逐像素保护。

### S1：眼白与虹膜识别证据收敛

当前 512 像素脸部 ROI 已进入后处理平台期。下一候选不扩大 c9f 的材料传播，而是提高眼部局部识别和回投证据。

**c10a 只读诊断结果**：c9l 的八眼眼白核心缺口共24像素，分别落在24个二级叶；22叶没有直接面部细节样本，2叶只有1个样本。真实分辨率探针中，1024只得到2个双样本眼白叶，仍有19叶无证据；2048得到14个双样本眼白叶，但仍有5叶无证据、3叶为可靠虹膜冲突、2叶为单样本。全视角1024的耗时／内存分别为512的1.39–1.58倍／1.38–1.52倍，2048为2.14–2.55倍／1.92–2.11倍，均超过20%预算。证据见 `.tmp/semantic-validation/canonical-gui-validation/r4-c10a-diagnostic/RESULT.md`。这支持选择性高分辨几何回投，不支持继续放宽材料传播或全视角提高分辨率。

**c10a 单一假设**：从512结果选择眼部证据最完整的一个正面视角，只对该脸部ROI增加2048几何回投。512继续负责稳定主体；2048结果只补充眼白和虹膜显式细节证据，不能替换512结果，眉毛保持冻结。全视角1024和2048方案已因收益不足与性能超标否决。

**实现切片**：

1. 先增加只读诊断，逐眼记录512裁剪中的有效眼白／虹膜叶、每叶样本数、可见视角和冲突标签；不改变输出。
2. 第一候选只把选中的一个人脸ROI重新渲染为2048，复用同一相机区域和原面／重心回投；实现时释放不再需要的RGB和深度缓冲，并记录增量峰值内存。
3. 2048证据与512证据按叶合并。只有高分辨结果中同一叶获得至少两个一致样本，且无虹膜、眼睑或肤色冲突时，才补充眼白；虹膜证据始终优先保留。
4. `pipeline_version`、渲染身份和缓存签名同步升级；缓存往返、取消、切换模型过期结果必须有测试。
5. c10a只修改识别证据，不修改耗材槽位、眼白色阈值、传播规则或子面预算。若性能或准确率不达标，再单独评估c10b局部偏航视图。

**融合规则**：

1. 保留原面 ID、深度和二级子面路径，不从真值 mask 或人物身份产生生产规则。
2. 虹膜证据优先否决眼白；眼睑／肤色证据阻止补白；不同视角冲突时弃权并继承 c9f 安全材料。
3. 高分辨证据只进入统一 `Label／confidence／samples` 契约，模型适配器外不暴露 MediaPipe 点号。
4. 更新识别和处理版本，使旧缓存强制失效；取消或切换模型后的旧任务不得写回。

**c10b 备用假设**：仅当 c10a 自动结果证明仍是遮挡而非采样密度问题时，增加少量局部偏航脸部视图；不得与 c10a 同一候选同时引入。

**诊断结论**：c10a单裁剪只能可靠覆盖刘亦菲左眼、双人男右眼和双人女左眼的各1个缺口；方飞左右眼和双人女右眼远低于98%。c10b在最佳视角附近加入`±10°/±20°`后虽看到更多眼白，但同一叶出现可靠虹膜／肤色冲突；按虹膜优先规则，安全收益仍不足。选择性2048峰值工作集仍为约`0.91–1.03 GB`。两项均在只读诊断阶段否决，未构建产品候选。详细证据见 `.tmp/semantic-validation/canonical-gui-validation/r4-c10a-diagnostic/RESULT.md`。

下一步将c11a眼部材料边界提前到眼部阶段：只处理眼白／虹膜／眼睑冲突叶，完成后再进入眉毛视觉重量。头发边缘和其他五官锯齿仍保持冻结。

**自动门槛**：

- 方飞左右眼、双人女性右眼分别达到 98% 眼白核心；其余五眼不得下降。
- 八眼新增确定误白、眼睑保护区误白、虹膜核心损失均为 0。
- 耳发 `ear-hair-v1`、眉毛、衣料、嘴唇、动物和固定区域变化均为 0。
- 记录触发视图数、额外处理时间和峰值内存；最终方案仍须满足相对 R0 增幅不超过 20%。

达到自动门槛后再交付用户检查完整双眼，不只截取改善点。用户确认眼白、虹膜、眼线和眼角后冻结为 `eye-material-v1`。

### S2：眉毛视觉重量

当前人像卡只有深色 `#282629`、中灰 `#958B86` 和白色等离散耗材；不存在介于中灰和白色之间的中性耗材。不得通过预览虚构不可打印的浅色。

**候选顺序**：

1. 先让用户在 `eye-material-v1` 上判断当前中灰眉毛是否可接受。
2. 若仍偏重，候选只收窄低置信眉毛外缘，让不确定叶继承皮肤材料；可靠眉毛核心继续使用真实中灰耗材。
3. 不把眉毛换成肤色或白色，不修改虹膜深色槽，不引入新的叠色物理模型。

**完成条件**：四个人物双眉均可见、视觉重量通过；眉外确定皮肤无新增灰色；眼白、虹膜和耳发冻结区逐像素不变。通过后冻结为 `eyebrow-material-v1`。

### S3：R4 材料边界锯齿

先处理五官，再处理头发边缘。该阶段修复可保存的材料轮廓，不使用 MSAA、模糊或法线平滑代替。

**c11a 眼部边界**：利用眼白、虹膜、眼睑的可靠矢量轮廓和多视角子面覆盖率，对被轮廓穿过的原面选择二级叶；冲突叶继承已冻结的安全材料。重点检查眼珠圆度、眼白／虹膜分界、眼角和眼线。

**c11b 眉毛与其他五官边界**：在 `eyebrow-material-v1` 上处理眉缘、嘴唇和鼻翼等确定轮廓；每个候选只处理一个标签族。

**c11c 头发边界**：仅对可靠 Hair／FaceSkin 或 Hair／Clothes 分界面使用二级叶，要求多视角一致和固有发色连续；保护耳肤、真实衣缝、灰白发和染发。

**统一门槛**：

- 继续遵守每原面最多 16 叶、新增三角形不超过原面数 20% 且不超过 200,000。
- 排除 R0 冻结不确定带后，目标轮廓偏移 P95 不超过 1 像素；同时报告最大错误连通片。
- 预览、人工整面覆盖、撤销、导入、3MF 保存重开和切片材料预览使用同一子面树。
- 超出两级子面能够表达的边界必须登记为网格拓扑限制，不能以屏幕平滑冒充通过。

### S4：R5 光照表面平滑

把现有 R5-a 保折角 45 度平滑法线实现重新合并到通过的 R4 基线。只处理开灯后的三角明暗块。

**完成条件**：原色和语义预览在开灯时眼球、眉弓和脸部表面连续；关灯材料分区逐像素等于 S3；网格位置、材料树、导入和保存结果不变；反面、重面、薄片和非流形边不串联。

### S5：R6 与 R7 最终闭环

材料效果冻结后合入无统计 MediaPipe 本地运行库，完成联网观察、断网、资源缺失回退、性能和包体验证。最后执行试色开关、耗材切换、人工纠错、撤销重做、切页恢复、取消、切换模型、原色／试色导入、另存重开和切片材料预览。

## 每个候选的固定交付

1. 候选假设和唯一主要变量。
2. 四模型固定六视角与目标区域原生 4 倍图。
3. 相对上一冻结点的面、子面和像素差分。
4. 目标指标、保护区、动物回退和性能结果。
5. Release 程序、运行资源哈希、源码检查点和双击入口。
6. 用户结论：通过、未通过或需继续观察。未收到用户截图结论前不得标记视觉通过。

## 候选队列与当前闸门

| 顺序 | 候选 | 唯一主要变量 | 当前状态 | 进入下一项的证据 |
| --- | --- | --- | --- | --- |
| 1 | c9l | 一次性三维耳肤桥接 | 用户确认仍有左耳串色；保留为当前基线 | 左耳残留转入 c11c，不能标为 `ear-hair-v1` |
| 1a | c9m | 低置信／Unknown耳缘落点 | 失败并撤回；目标 27 像素不变 | 不再扩大半径或传播层数 |
| 2 | c10a-diagnostic | 512／1024／2048眼部叶采样报告 | 已完成，只读 | 八眼逐叶缺口、逐视角冲突和性能已复现 |
| 3 | c10a | 单个最佳视角的2048几何回投，与512合并 | 诊断否决，未构建产品候选 | 方飞双眼和双人女右眼收益不足，峰值内存超标 |
| 4 | c10b | 少量局部偏航视图 | 诊断否决，未构建产品候选 | 多视角眼白与虹膜／肤色冲突，安全收益不足 |
| 5 | c11a | 眼白／虹膜／眼睑二级叶轮廓 | 当前下一候选 | 八眼轮廓P95≤1像素，最大错误片达标，保存重开一致 |
| 6 | c10c | 眉毛低置信外缘收窄 | 等待`eye-material-v1` | 四人双眉可见、用户接受，冻结眼和耳逐像素不变 |
| 7 | c11b | 其他五官边界，每次一个标签族 | 等待c11a | 五官保护区与材料树门槛通过 |
| 8 | c11c | Hair／Skin、Hair／Clothes边界 | 等待c11b | 发际线与发衣边界通过，耳肤和真实衣缝不回退 |
| 9 | R5-b | 45度保折角平滑法线合并 | 等待R4冻结 | 开灯表面连续，关灯材料分区逐像素不变 |
| 10 | R6/R7 | 无统计运行库与产品闭环 | 等待视觉算法冻结 | 网络、断网、回退、性能、编辑／导入／保存全门槛通过 |

## 立即执行点

当前进入 c11a：c10a和c10b均被逐视角诊断否决，不能用高分辨率强行覆盖可靠虹膜冲突。c11a只解决眼白／虹膜／眼睑的材料边界；完成后再调整眉毛。刘亦菲左耳、头发边缘和其他五官锯齿继续保留为R4-C5后续反例，R5不提前混入。
