notes.ignore

2026/06/04
TODO:
  - Implement reporting an error message if printer is busy while uploading.
      Error uploading file: , HTTP 405, body: `{"message": "Method not allowed"}`

  - Implment the UltiMaker cloud api (separate PR)
    - Implement queueing.
  
  - Create other process profiles (0.27mm, 0.11mm, 0.08mm, etc etc etc.)
  
  - Use boost formatting correctly
  - Fix the logging levels
  - Create PR
    - Merge main into local
    - Compile, make sure everything works
    - Create the PR



2026/06/03
TODO:
  - Implement reporting an error message if printer is busy while uploading.
      Error uploading file: , HTTP 405, body: `{"message": "Method not allowed"}`

  - Implment the UltiMaker cloud api (separate PR)
    - Implement queueing.
  
  - Fix bug where machine appears offline in cura cloud thing when printing custom gcode
  - Create other process profiles (0.27mm, 0.11mm, 0.08mm, etc etc etc.)

  - ~~Implement authentication key generation autofill~~ (cancelled)
  - ~~Fix the "Upload" button and make it just "Print" button only~~ (cancelled, too difficult to implement)
  
  - Use boost formatting correctly
  - Fix the logging levels
  - Create PR
    - Merge main into local
    - Compile, make sure everything works
    - Create the PR


{"id": "7e2f509e80fc7081b9f8a7e8a1f6b153", "key": "f745a3269a276cf8b6f78d1940414b018461003f700374674a886c6f7e67480f"}

2026/06/02
TODO:
  - Implement reporting an error message if printer is busy while uploading.
      Error uploading file: , HTTP 405, body: `{"message": "Method not allowed"}`

  - Implment the UltiMaker cloud api (separate PR)
    - Implement queueing.
  
  - Fix bug where machine appears offline in cura cloud thing when printing custom gcode
  - Create other process profiles (0.27mm, 0.11mm, 0.08mm, etc etc etc.)

  - Fix auth cred generation (get to post request causes crash.... why????)
    - Implement authentication key generation autofill
  
  - ~~Fix the "Upload" button and make it just "Print" button only~~ (cancelled, too difficult to implement)
  - Use boost formatting correctly
  - Fix the logging levels
  - Create PR
    - Merge main into local
    - Compile, make sure everything works
    - Create the PR



2026/05/13
TODO:
  - Implement reporting an error message if printer is busy while uploading.
      Error uploading file: , HTTP 405, body: `{"message": "Method not allowed"}`
  - Implement queueing.
  - Implement authentication key generation autofill
  - Fix bug where machine appears offline in cura cloud thing when printing custom gcode
  - Create other process profiles (0.27mm, 0.11mm, 0.08mm, etc etc etc.)
  - Finish tuning the MatterHackers PLA
  - Use boost formatting correctly
  - Implement Ultimaker cloud api
  - Create PR
    - Merge main into local
    - Compile, make sure everything works
    - Create the PR



2026/03/30
TODO:
  - Implement reporting an error message if printer is busy while uploading.
  - Implement queueing.
  - Implement authentication key generation and autofill stuff
  - Fix bug where machine appears offline in cura cloud thing when printing custom gcode
  - Create other process profiles (0.27mm, 0.11mm, 0.08mm, etc etc etc.)
  - Finish tuning the MatterHackers PLA
  - Use boost formatting correctly

10.125.203.43
{
  "id": "08cf26733f93231c9bc8c25a30b3c067",
  "key": "e837eaa9e3262a6ce9d3bbdc2924050fa2ccffd24cfc282911f8a01fc73fbddd"
}


2026/03/23
TODO:
  - Implement reporting an error message if printer is busy while uploading.
  - Implement queueing.
  - Implement authentication key generation and autofill stuff
  - Fix bug where you have to select "Duet" type of machine in UI to get Ultimaker code.
  - Fix bug where machine appears offline in cura cloud thing when printing custom gcode
  - Decide whether to use Ultimaker or UltiMaker capitalization and standardize that everywhere its used
  - Create other process profiles (0.27mm, 0.11mm, 0.08mm, etc etc etc.)
  - Finish tuning the MatterHackers PLA


2026/03/20
TODO:
  - Implement queueing.
  - Implement reporting an error message if printer is busy while uploading.
  - Fix bug where you have to select "Duet" type of machine in UI to get Ultimaker code.
  - Fix bug where logging below "warning" level doesn't work.
  - Implement authentication key generation and autofill stuff
  - Fix the bed model
  - Fix time in gcode headers
  - Decide whether to use Ultimaker or UltiMaker capitalization and standardize that everywhere its used
  - Create other process profiles (0.27mm, 0.11mm, 0.08mm, etc etc etc.)
  - Finish tuning the MatterHackers PLA



2026/02/11

TODO:
  - Implement sending jobs and queueing.
  - Fix bug where you have to select "Duet" type of machine in UI to get Ultimaker code.
  - Fix bug where logging below "warning" level doesn't work.
  - Implement authentication key generation and autofill stuff
  - Fix the bed model
  - Decide whether to use Ultimaker or UltiMaker capitalization and standardize that everywhere


Roebling auth credentials:
{
  "id": "8b6b6d5e4b27314c0051d109c18c8f04",
  "key": "f9627c877718a05fe8408403a972600c5dc31db6537c11afd963c99c7f4e168f"
}
97608a341486c8eb04c438540a6f72d8
33c92feaf6672c9e5e2b3c00317b3d70bef7e5c35f8373a69b34b1efb266c843

New auth flow?
1. user creates the machine, fills out ip address
2. user hits "test" button.
    Test protocol:
     - test basic connection to printer
     - test auth via /auth/check/{id}
       - if no auth key, fail and tell user
       - if authorized, pass.
       - if unauthorized, fail and tell user
       - if "unknown" (user needs to hit the button on the machine), fail and tell the user.

3. user hits generate creds button.
    Generate creds protocol:
    a. if auth creds already filled out, fail and tell user. Otherwise continue
    b. test connection to printer, fail on failure and pass along err message.
    c. make the actual http post request to /auth/request
    d. auto fill out fields of the machine to match returned id (username) and key (password)
    e. tell user they have to approve using the physical button and exit.


2025/11/24
OK build seems to actually work now, don't touch it or pull from main.

TODO:
  - Fix bug where you have to select "Duet" type of machine in UI to get Ultimaker code.
  - Fix bug where logging below "warning" level doesn't work.
  - Implement authentication
    - Rework the UI to accomodate fields (id and key/password)
    - Remembering old authentication
    - Requesting a new id for auth automatically.
    - Process:
      - User creates a wifi-enabled machine using the UI
        1. Enters IP of printer and authentication credentials (optional, id and password)
        2. Clicks Test button
        3. Test routine checks authentication status.
          - If authenticated:
            - Do nothing
          - If unauthenticated and no creds given:
            - Go to authentication creation process.
          - If unauthentcated and creds given (invalid creds):
            - Report a specific error to the user to say that the creds are invalid.
      - Authentication creation process:
        1. Check if creds already exist.
        2. Send POST request to /auth/request to request authorization
        3. Get the ID and Key from the request.
        4. Send GET request to /auth/check/{id} to check status of authentication
        5. While user hasn't hit the button yet (previous return message is "unknown"):
          - Display message to user telling them to authorize on the printer.
          - wait a second
          - Recheck the auth status
          - Break the loop if user hits "cancel" button in UI
        6. If "authenticated", success.
          - Save the credentials somewhere.
          - Return success
        7. If "unauthentcated", it means user hit "no." Display test failed message.
  - Fix queueing (get "413 request entity too large" or something like that)



2025/10/13
Before submitting the pull request, make sure to increment the version numbers of all process 
and machine profiles, and all .json machine profiles.

Note that the .config folder is /var/home/dwingfield/Ubuntu24_04_Orca_Home/.config

2025/10/12
Pro tip: If getting undefined reference errs, try checking the CMakeLists file and adding 
the appropriate files to the correct lists.

If getting the "g_signal_handlers_disconnect_matched: assertion 'G_TYPE_CHECK_INSTANCE (instance)' failed"
error (on running the appimage), try making sure that the path "/var/home/dwingfield/Ubuntu24_04_Orca_Home/.config"
exists. If not, make the .config dir in that location so OrcaSlicer can create the OrcaSlicer subdir.


2025/04/02

User selects Ultimaker machine, fills out IP, runs "test."
Test
  - Connect to Ultimaker
    - Authenticate
      - See if credentials already exist
        - If so, use credentials
        - If not, request authentication and prompt user to approve on machine
      - Verify credentials
    - Get printer info (name, version, uptime, status, etc.)


ALTERNATE: look up volumes stuff on https://docs.docker.com/engine/storage/volumes/ for "no matchine entries in passwd file" errors.
Create a volume before referencing it.

Note: to avoid user not found errors, full docker run command (in DockerRun.sh) should be 
docker run \
  `# Use the hosts networking.  Printer wifi and also dbus communication` \
  --net=host \
  `# Some X installs will not have permissions to talk to sockets for shared memory` \
  --ipc host \
  `# Bind mount your home directory into the container for loading/saving files` \
  -v ./home:/root \
  `# Pass the X display number to the container` \
  -e DISPLAY=$DISPLAY \
  `# It seems that libGL and dbus things need privileged mode` \
  --privileged=true \
  `# Attach tty for running orca slicer with command line things` \
  -ti \
  `# Clean up after yourself` \
  --rm \
  `# Pass all parameters from this script to the orca slicer  ENTRYPOINT binary` \
  orcaslicer $* 

