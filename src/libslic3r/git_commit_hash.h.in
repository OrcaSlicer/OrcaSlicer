#ifndef __GIT_COMMIT_HASH_H
#define __GIT_COMMIT_HASH_H

// Generated by CMake from git_commit_hash.h.in — do NOT edit manually.
// Kept separate from libslic3r_version.h (which is included in the PCH)
// so that changing the git commit (e.g. switching branches) only recompiles
// the few translation units that include this header, not the entire project.
#define GIT_COMMIT_HASH "@GIT_COMMIT_HASH@"

#endif /* __GIT_COMMIT_HASH_H */